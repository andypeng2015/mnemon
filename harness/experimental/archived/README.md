# Archived Harness Surfaces

This tree keeps older eval and goal loop declarations, host assets, bindings,
and shell projectors for proof-only reference.

They are intentionally outside the normal declaration roots:

- `harness/loops`
- `harness/bindings`
- `harness/hosts`

Normal Agent Integration setup validates and installs memory and skill only.
